name: Price Update Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  update-price:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 📌 1. pip 패키지 캐시
      - name: Cache Pip Packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 📌 2. Playwright 브라우저 바이너리 캐시
      - name: Cache Playwright Browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright
          restore-keys: |
            ${{ runner.os }}-playwright

      # 📌 3. HuggingFace BERT 모델 파일 캐시
      - name: Cache HuggingFace Models
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: ${{ runner.os }}-hf-models
          restore-keys: |
            ${{ runner.os }}-hf-models

      # 📌 4. 통합 라이브러리 및 브라우저 설치 (openai 패키지 추가)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install gspread google-auth google-auth-oauthlib beautifulsoup4 curl_cffi google-genai tenacity playwright sentence-transformers torch Pillow openai
          playwright install chromium

      # 📌 5. 가격 업데이트 스크립트 실행 (OPENAI_API_KEY 추가)
      - name: Run price update script
        env:
          GCP_TOKEN_JSON: ${{ secrets.GCP_TOKEN_JSON }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python update_price.py
